steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [
    'build',
    '--build-arg', 'NEXT_PUBLIC_API_URL=$_NEXT_PUBLIC_API_URL',
    '-t', 'europe-west2-docker.pkg.dev/my-tele-pa/sage/frontend',
    '.'
  ]
images:
- 'europe-west2-docker.pkg.dev/my-tele-pa/sage/frontend'
